# Reusable GitHub Actions

These are reusable actions for GitHub workflows.

Unfortunately, GitHub makes it really hard to use a private repository in workflows as
a source for reusable tasks. Either it requires a lot of setup for every repository,
or it's insecure. Instead, we'll just copy these around to the repos.